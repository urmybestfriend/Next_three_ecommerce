import React from 'react'

const TopBar = () => {
    return (
        <div id="topbar" className="docs-item element">
            <h5 className="text-uppercase mb-4">Top Bar</h5>
            <div className="docs-desc">
                <p className="lead">Top bar element that precedes the navbar, as seen also on this page. Great to display telephone number, language or currency choice or similar.</p>
            </div>
        </div>
    )
};

export default TopBar;